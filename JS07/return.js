function tenTimes(number){
    const result = number * 10;
    return result;
}

// if you use return on a function then you have to asssign this function in a variable so that you can call it and see it on the console otherwise you woun't see it E.g. on the bottom -->

const retrn = tenTimes(5)
console.log(retrn)